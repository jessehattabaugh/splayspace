exports.handler = async function handleMessageStream(event) {
	console.log('🚀', 'message', event);

	// get all the players and loop over them
	//   send each one a message on their websocket connectionId

	return;
};
